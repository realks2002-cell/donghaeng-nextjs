{
  "name": "donghaeng-android",
  "version": "1.0.0",
  "private": true,
  "description": "행복안심동행 Android 하이브리드 앱",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/input.css -o ./www/css/styles.css --minify",
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build": "npm run build:css && npm run sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/share": "^6.0.4",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
